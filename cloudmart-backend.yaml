apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudmart-backend-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudmart-backend-app
  template:
    metadata:
      labels:
        app: cloudmart-backend-app
    spec:
      serviceAccountName: cloudmart-pod-execution-role
      containers:
      - name: cloudmart-backend-app
        image: CONTAINER_IMAGE_BACKEND
        env:
        - name: PORT
          value: "5000"
        - name: AWS_REGION
          value: "us-east-1"
        - name: BEDROCK_AGENT_ID
          value: "REPLACE_BEDROCK_AGENT_ID"
        - name: BEDROCK_AGENT_ALIAS_ID
          value: "REPLACE_BEDROCK_AGENT_ALIAS_ID"
        - name: OPENAI_API_KEY
          value: "REPLACE_OPENAI_API_KEY"
        - name: OPENAI_ASSISTANT_ID
          value: "REPLACE_OPENAI_ASSISTANT_ID"
        - name: AZURE_ENDPOINT
          value: "REPLACE_AZURE_ENDPOINT"
        - name: AZURE_API_KEY
          value: "REPLACE_AZURE_API_KEY"
---

apiVersion: v1
kind: Service
metadata:
  name: cloudmart-backend-app-service
spec:
  type: LoadBalancer
  selector:
    app: cloudmart-backend-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
